# Today Tactics — Python 의존성
# 생성 기준: 2026-04-15 (Python 3.14)
#
# 설치: pip install -r requirements.txt
# Playwright 브라우저 바이너리 추가 설치: playwright install chromium

# ── Core runtime (main.py, update_data.py, crawlers/) ──
flask==3.1.3
playwright==1.58.0

# ── Analysis only (analysis/*.py, 읽기 전용 분석 스크립트) ──
# 프로덕션 Flask 서버 구동에는 불필요. 필요 시에만 설치.
pandas==3.0.2
numpy==2.4.4
matplotlib==3.10.8
seaborn==0.13.2
scipy==1.17.1
scikit-learn==1.8.0
