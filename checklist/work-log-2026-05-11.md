# 작업 기록 — 2026-05-11 (5/8~5/11 통합)

## 세션 개요
- **시작 시점**: 5/7 종료 시점 — update_data.py 15 STEP cron, 전술 보기 UI 가동, 데이터 정합성 99%+
- **종료 시점**: P7(모의해킹) 페르소나 도입 + HTTPS + 보안 헤더 + rate limit, OAuth 3종 로그인, 전술 카드 race 근본 해결(Promise.all), match-prediction 500 픽스
- **commits**: 5/8 28건 + 5/11 2건 = 30건
  - 5/8: `52a3c29 → 21dd16c → ec865a8 → 4041d08 → eba5142 → 868d8b3 → 86882e1 → 93f4020 → 52b3228 → 31d4494 → 44671a3 → 96abf75 → f26d00e → bfbce34 → adb83bf → 064de37 → 5045808 → 705c1c4 → d539574 → 0a08fcb → 4262c8a → 95a98eb → 1ffa4a0 → 62efebf → 0b3779d → c55570d → 1c90fd8 → 41c6818 → fdc58b7`
  - 5/11: `20e0f5d → 640e2c4`

---

## 1. 보안 / P7 페르소나 도입

### 1-1. P7 모의해킹 페르소나 + 인증·헤더·rate limit 보강 (commit `21dd16c`)
- **CLAUDE.md**: 6인 → 7인 관점 체크. P7(OWASP Top 10, SQLi/XSS/CSRF/SSRF, 인증·세션, 비밀정보 노출, DB 권한, 공급망) 추가. `@security` 태그 신설. 자율 판단 매트릭스에서 P7은 어떤 등급이든 무조건 Red 오버라이드.
- **`/api/trigger-update` fail-closed**: 기존엔 `UPDATE_SECRET` 미설정 시 검증이 우회되어 누구나 update pipeline 트리거 가능. 환경변수 미설정 시 503 반환으로 변경. `hmac.compare_digest`로 타이밍 공격 방어.
- **nginx 보안 헤더**: X-Frame-Options(SAMEORIGIN), X-Content-Type-Options(nosniff), Referrer-Policy(strict-origin-when-cross-origin), Permissions-Policy(geo/mic/cam 차단), `server_tokens off`, `client_max_body_size 1m`.
- **rate limit**: 일반 60r/m, `/api/trigger-update` 5r/h. 정적 자원에도 nosniff 적용.
- `.update_secret_local.txt` → `.gitignore`.

### 1-2. nginx 단위 / default_server 픽스 (commits `ec865a8`, `4041d08`)
- `r/h`는 nginx 미지원 단위 → `r/m`으로 정정 (5r/h → 1r/m).
- `today_alarms.conf`와 80 포트 동거. IP 직접 접근 시 alphabetical로 alarms가 먼저 매칭되어 today_tactics 404. `listen 80 default_server` 명시.

### 1-3. HTTPS 활성화 (commit `868d8b3`)
- 도메인 `today-tactics.co.kr` cert는 certbot으로 이미 발급되어 있음.
- 443 default_server에 today_tactics 라우팅, 80 → 443 301 redirect, `/.well-known/acme-challenge/`는 평문 유지(갱신용).
- HSTS `max-age=31536000; includeSubDomains` 활성화.
- proxy에 `X-Forwarded-Proto $scheme` 추가 → Flask가 https 인식.

### 1-4. rate limit 완화 (commit `0b3779d`)
- 60r/m이 정상 사용자 페이지 로드(여러 자원 동시 요청)를 차단 → 한계 상향.

---

## 2. 성능 / 인프라

### 2-1. gunicorn workers 3 + nginx gzip/http2 (commit `93f4020`)
- gunicorn 1 worker → 3 worker (2 vCPU + 1).
- nginx http/2, gzip on (text/css, application/javascript, application/json, image/svg+xml), 정적자원 long expires (`Cache-Control: public, max-age=2592000`).

### 2-2. service unit User/venv 경로 수정 (commit `52b3228`)
- 배포 unit 파일의 가상의 사용자/경로를 실제 서버 환경(가비아 g2 Rocky 9)에 맞게 정정.

### 2-3. CPU 우선순위 상향 (commit `1ffa4a0`)
- `today_tactics.service`에 `CPUWeight=300`(default 100), `Nice=-5` 추가. 같은 호스트의 `today_alarms` 대비 응답 우선권 확보.

### 2-4. index.html no-cache 헤더 (commit `62efebf`)
- index만 캐시 비활성(`Cache-Control: no-store`). cache-bust query(`v=N`)가 항상 최신 정적 자원을 가져오도록 보장. 정적 자원은 long expires 유지.

---

## 3. 로그인 시스템 (OAuth 3종)

### 3-1. Google / Kakao / Naver OAuth (commit `064de37`)
- **Backend (main.py +257 lines)**:
  - Authlib OAuth 클라이언트 3종 (Google OIDC, Kakao, Naver).
  - 라우트: `/auth/login/<provider>`, `/auth/callback/<provider>`, `/auth/logout`, `/auth/me`.
  - `before_request` 게이트로 일괄 보호. `/login`, `/static/`, `/auth/`, `/health`, `/favicon`만 무인증 허용. 페이지는 `/login` 리디렉트, API는 401 JSON.
  - `users` 테이블 신설 (provider, sub, email, name, picture, timestamps). 콜백 시 `_upsert_user`로 INSERT/UPDATE.
  - 세션: `HttpOnly` + `Secure`(prod) + `SameSite=Lax` + 30일 TTL.
  - `FLASK_SECRET_KEY` 환경변수(없으면 부팅 시 임시 생성 — 재기동 시 세션 무효).
  - `FLASK_DEV=1` 시 Secure 쿠키 비활성 (로컬 http 개발).
- **Frontend**:
  - `templates/login.html` — 다크 테마, 3개 provider 버튼 (env 미설정 시 disabled 표시).
  - index 헤더에 사용자 메뉴 (avatar + name + 로그아웃).
- `requirements.txt`에 Authlib 추가.

### 3-2. LOGIN_REQUIRED 환경변수 토글 (commit `5045808`)
- 강제 로그인을 env로 ON/OFF. 데모/개발 시 잠금 해제 가능. 기본 ON.

---

## 4. 전술 카드 race condition 근본 해결

5/7 작업분 이후 사용자 보고: "특정 매치에서 전술 보기 카드가 보였다 안 보였다 한다". 3단계 시도 끝에 근본 원인 제거.

### 4-1. 1차 시도 — 매치 active 피드백 + 즉시 fetch (commit `44671a3`)
- 매치 카드 클릭 시 `.kmc-active` 클래스 토글 → 시각 피드백.
- 매치 전환 race: 직전 fetch 완료 시점에 다른 매치 선택 시 잘못된 렌더링 발생 → `_lastHome/_lastAway` 가드.

### 4-2. 2차 시도 — pending 보관 (commit `d539574`)
- match-extras 응답이 render() 완료 전 도착하면 `_pendingTactics`에 보관 → render 완료 후 `tryRenderPendingTactics()`로 재시도.

### 4-3. finished 무관 fetch (commit `0a08fcb`)
- 기존엔 `isFinished`일 때만 extras 시도 → 진행 중/예정 매치에서 데이터가 있어도 미표시. 항상 fetch 후 응답에 따라 표시.

### 4-4. 3차 (근본) — Promise.all 통합 (commit `41c6818`)
- 위 펜딩 흐름이 `workers=3` + `HTTP/2` 환경에서 여전히 일부 케이스 미표시.
- **조치**: match-extras를 `loadPrediction`의 `Promise.all`에 포함. render() 직후 동기적으로 `renderTacticsCard` 호출 → race 자체 제거.
- `_pendingTactics`, `tryRenderPendingTactics` 등 펜딩 흐름 전부 삭제. click handler 단순화.

### 4-5. cache-bust (commit `96abf75`)
- prediction.js / insights.js / style.css 버전 일괄 상향 (race fix 즉시 반영).

---

## 5. 전술 보기 UX 개선

### 5-1. 교체 패널 + 선발/교체 점 시각 구분 (commit `4262c8a`)
- `/api/match-extras` 응답에 `subs` 추가. `substitution_events` 테이블 부재 → mps의 `mins` 기반 추정: `out.mins ≈ 90 - in.mins` 같은 시각 페어 zip. minute = `out.mins`(정확), IN만 있으면 `90 - in.mins` 근사.
- 평균 포지션 위에 홈/원정 분리 교체 리스트 (시각 + OUT(빨강 취소선) → IN(녹색)).
- "교체 포함" 체크박스 제거 — 항상 모두 표시.
- 선발(채운 원, 큰 점) vs 교체 IN(점선 작은 원, 반투명) 시각 구분.
- 헤더에 범례 추가 (선발/교체 IN 샘플).

### 5-2. 자동 업데이트 위젯 UI 제거 (commit `bfbce34`)
- P7 인증 적용 후 일반 방문자는 ↻ 클릭 시 403 → UX 혼란.
- 사용자 결정으로 위젯 자체 제거. API(`/api/trigger-update`, `/api/update-status`)는 유지 (관리자가 서버 curl 가능). cron pipeline은 무관.
- 제거: `#auto-update-widget` 마크업 + IIFE 폴링 + `.auw-*` CSS.

---

## 6. 데이터 수집 패치

### 6-1. backfill_k1_mps `mins NULL` 케이스 (commit `1c90fd8`)
- 출전 시간이 NULL인 케이스에서 backfill이 스킵되어 일부 K1 mps 비어있던 버그. NULL → 0 처리하여 정상 백필.

---

## 7. 5/11 핫픽스 — match-prediction 500

### 7-1. setpiece_analysis 500 수정 + H2H 폴백 추가 (commit `20e0f5d`, Sonnet 4.6 작성)
- **버그**: `/api/match-prediction` 호출 시 500 — `setpiece_analysis()`가 `goal_events` 테이블 미존재 시 OperationalError.
- **수정**: `sqlite_master`에서 테이블 존재 확인 후 쿼리. 미존재 시 0 채워 반환.
- **추가**: 전술 보기 H2H 폴백 (Fallback1: 직전 H2H 맞대결, Fallback2: 각 팀 최근 경기 개별 조합). prediction.js Promise.all 에러 핸들링 강화, render() try-catch, fallback 배지 (`pt-fallback-badge`).

### 7-2. H2H 폴백 제거 (commit `640e2c4`)
- **사용자 판단**: 예정 경기에서 H2H/최근경기 데이터를 보여주면 오해 소지. 과거 완료 경기만 데이터 반환하도록 폴백 100여 줄 롤백.
- setpiece_analysis 500 수정은 유지 (이게 근본 문제였음).
- prediction.js fallback 배지 제거 (v33).

---

## 8. 핵심 발견 / 교훈

1. **race fix는 가드보다 통합** — pending 보관 + 가드 변수 + 재시도 루프로 막아도 멀티 worker + HTTP/2 환경에선 빈틈. 같은 시점에 함께 fetch해 `Promise.all`로 묶는 게 근본 해결.

2. **fail-closed 원칙** — `UPDATE_SECRET=None`일 때 검증 우회로 누구나 트리거 가능했던 패턴. 환경변수 미설정은 "허용"이 아니라 "거부(503)"로 처리.

3. **default_server 명시** — 같은 호스트의 다른 site config와 80 포트 공유 시 alphabetical 매칭에 의존하면 IP 접근/매칭 안 되는 호스트가 엉뚱한 site로 라우팅됨. 의도하는 site에 `default_server` 명시 필수.

4. **데이터 부족한 폴백은 UX 함정** — 예정 경기에서 H2H 데이터를 보여주는 게 친절해 보이지만, 사용자는 "이 경기 예측 데이터"로 오해. 데이터 없으면 그냥 미표시가 정직.

5. **인증 도입은 UI 정리와 묶기** — P7 인증 적용하면서 노출돼있던 ↻ 위젯이 403 받게 됨. 보안 보강은 항상 "이제 동작 안 하는 UI" 정리와 한 세트.

6. **HTTPS는 인증보다 먼저** — OAuth 3종 도입 전에 HTTPS + Secure 쿠키 + SameSite=Lax + HSTS를 먼저 확정. 평문에서 세션 도입하면 첫 트래픽이 위험.

---

## 9. 잔여 백로그

### P1 — 없음
P7 권고는 이번 세션에 대부분 처리됨.

### P2 (선택)
- [ ] **CSP(Content-Security-Policy) 헤더** — 현재 다른 보안 헤더는 다 있지만 CSP 미적용. inline script 다수라 nonce/hash 도입 작업량 큼.
- [ ] **CSRF 토큰** — 현재 SameSite=Lax + cookie session으로 1차 방어만. POST API(`/api/saves`, `/api/squads`)에 명시적 토큰 검토.
- [ ] **substitution_events 테이블 신설** — 현재 mps mins 추정 기반. SofaScore `/event/{id}/incidents`에서 정확한 교체 시각/IN/OUT 페어 직접 수집.
- [ ] **OAuth 콜백 URL allowlist 점검** — Google/Kakao/Naver 콘솔에 등록된 redirect_uri 환경별(prod/dev) 명확화.
- [ ] **5/7 잔여**: 5/22 미래 K1 매치 1건, K1 xG 백필, 5/5 R11 3매치 히트맵.

### P3 (의식적으로 안 함)
- ❌ CSP 강제 적용 — inline script 전수 리팩터링 필요, 우선순위 낮음
- ❌ 세션 DB 저장(redis) — 현재 cookie-only로 충분, scale 시점 재검토

---

## 10. 상태 요약

### 인프라
- **도메인**: today-tactics.co.kr (HTTPS, HSTS)
- **gunicorn**: 3 workers, CPUWeight=300, Nice=-5
- **nginx**: http/2, gzip, 보안 헤더 6종, rate limit 60r/m (일반) + 1r/m (trigger)
- **인증**: Google / Kakao / Naver OAuth, before_request 게이트, 30일 세션

### `/api/` 신규/변경
- 신규: `/auth/login/<provider>`, `/auth/callback/<provider>`, `/auth/logout`, `/auth/me`
- 변경: `/api/match-extras` — subs 필드 추가, H2H 폴백 시도→롤백 (예정 경기는 미표시)
- 변경: `/api/match-prediction` — setpiece_analysis goal_events 미존재 가드
- 변경: `/api/trigger-update` — fail-closed (UPDATE_SECRET 미설정 시 503)

### DB 스키마 변화
- `users` (신규): provider, sub, email, name, picture, timestamps

### UI 변화
- index 헤더: ↻ 위젯 제거, 사용자 메뉴(avatar + name + 로그아웃) 추가
- 전술 보기: 교체 패널, 선발/교체 점 시각 구분, 범례 추가
- 매치 카드: `.kmc-active` 시각 피드백
- 전술 카드: race condition 근본 제거 (Promise.all 통합)
